# Speech-to-Scene Project Instructions

Follow `AGENTS.md` for scope, architecture boundaries, checks, and repository hygiene.

The real LLM implementation is provider-based. DeepSeek is the first preferred provider; Anthropic is optional. Do not bind Domain or Application code to a specific model SDK, model name, JSON-output feature, or API base URL.

The current milestone is recorded in `README.md`. Complete only the explicitly requested milestone and do not continue automatically into the next one.
